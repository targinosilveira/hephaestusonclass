<%= form_for(@server) do |f| %>
  <% if @server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@server.errors.count, "error") %> prohibited this server from being saved:</h2>

      <ul>
      <% @server.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nome do Servidor" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "IP do Servidor" %><br />
    <%= f.text_field :ip %>
  </div>
  <div class="field">
    <%= f.label "Porta SSH" %><br />
    <%= f.text_field :ssh_port %>
  </div>
  <div class="actions">
    <%= f.submit "Cadastrar" %>
  </div>
<% end %>
